# 2026-08-25 night batch 2 — Ayman's request, verbatim

Captured word-for-word at Ayman's explicit instruction ("store my entire
instructions/prompt in a file word by word so you can refernece"). This file is
the scope authority. Typos and all — do not "clean up" this text, do not
paraphrase it, and do not resolve its ambiguities inside this file. Open
questions and rulings live in `2026-08-25-night-batch-2-PLAN.md`.

Received 2026-08-25 23:03 CDT. One image attached (the Work schedule module,
referenced in item 4 as "[Image #10]").

---

I also need you to add the following changes/additions/improvements to tonights task list:

1. In the Home screen, instead of the subtitel where it says "Today's 5 prayer's are accounted for", replace this with Today's Schedule: then list main event's in order of occurance (main events would just include classes and work), so if it was monday it shoudl display "You have 1 class and work today", if it was tuesday it would say "You have 3 classes today", etc. Also in this same module, I need you to make the blobs at the bottom (the class and work ovals) actually span the correct time according to when they start and end and match with the time on the line itself, they shoudltn all be the same size, they shoudl match accoridng their size

2. There is an issue when it comes to logging prayers and logging tasks from pretty much any screen and domain, this relates to when i have the app open on my laptop and my phone, and i make a change on my phone, it doesnt display at all on my macbook, and vice versa, the changes shoudl display/update right away

3. Also, in the fitness seciton, i need you to remove the Hit protein target and 8k+ steps and log today's weight and log waist things from the daily log, only keep log waist and log weight but keep them in the cycle progress checks sections and dont turn them into daily tasks, just keep them there, when i want to do it I will.

4. iN hte work screen, in teh work schedule, i need you to highlight the current day ont eh schedle of wahtever the current day is, like how it is done in the school screen with "This week's classes' module. Also some of the logic behind this module is incorrect because it allows you to create events with their own titles, but this scheudle is just for work, this isnt to add new events, there should only be an edit button at the top right to edit work hours specifically, and when this edit button is rpessed, a popup window should appear with the schedule, and the abiltyt ot add/edit/remove work hours permanently, but also temporarily for this week or next week, and right now you see how in the day it says "Work" [Image #10] for every single day i have work, ofcourse its work, remove "Work" and instead put the actual timings of when work is during that day, but keep the cancel this week button, that is helpful, alos verify to make sure that actually works

5. In the school screen, I want you to combine the deadlines module and task list module into one module wiht the follwoing description/functionality: so this module should be called Task list, and it shoudl have 4 groups, Today, This Week, This Month (as in calendar month), and Future (anything beyond). under each group should be the sepciifc tasks listed in order of occurance (so from top to bottom should be in order of recency, so the sooner a task is the more up it should be placed). All groups should be collapsable and expanded, however by default, all gorups except 'Today' group should be collapsed so their tasks wotn be visible unless expanded, the Today group's task list shoudl be expanded by default but can be collapsed/re-expanded as well. Now in the top right there should be two buttons, an Add button, and an Edit button, pressing the edit button woudl open up a popup window allowing the user to remove any task from any gropu, or change the contents of any task as well. When the Add button is pressed, it will open up a popup prompt that will make the user first choose one of the 5 classes I'm taking or a generic school related task (just call it Generic), once teh specific class or generic is picked, in the same popup it shoudl then move onto the next prompt which should prmopt the user for the type of this task from the following options (Homework/Assignment, Quiz, Exam, Final/Midterm, Project/Paper, Reminder, Other (custom input)), once a type is selected, it will then move onto the 3rd and final step of the task addign popup where the user will enter the descriptiopn of the task as well as set a Date (for Homework/Assignmetn and Project/Paper the title of this input field should say 'Due Date' but for quizzes and exams it should just say 'Date') and there should be a fully working and convenicnet to use date picker. After this last step is entered, on the third screen (description and date) there should be an 'Add" button at the bottom, when this is pressed the popup goes away and the task gets added accordingly. Now inside the Task list module, there should be 3 filtering options: by class, by type, and by date. Whenever these are entered the list shoudl udpate accordingly, and whenever a filter is selected there shoudl be a deselect filters button that pops up to auotamtically deselect all filters, but the user should still be able to go into the drop downs and individually deselect any filters as well, and the resulst shoudl automatially work across all groups in teh task list (just because filters are present doesnt mean the groups besdies 'Today' autoamtically get auto expanded, they still filter but still stay collapsed). (also each task type should be a different text color)

6. So in the previous prompt I told you to make several changes in the School screen, one of them was to chagne the top modules and also make them smaller (as in basicaly make them look like the sector progress mini moudles in the home screen, make them look like that if they dont already), now in the school screen, below the top mini modules, I need you to add a module card for every single class im taking (Prob & Stats (CS 3341), DSA (CS 3345), Phys (PHYS 2326), Ameri Studies (AMS 2341), Phys Lab (PHYS 2126), Lin Alg (MATH 2418)). Add an individual card for each of these classes, inside teh card/preview, add the name of the class in the abbreivated forms i prvodied, then below that in slightly smaller text add the actual class name/code, and its room and the teacher, then add a task list number specific to tasks needed to be completed during this week (this shoudl jsut be a number), then next to that add an upcoming test seciton with a test name and test date, and in the top right should be a "View" button, when pressed, it shoudl open an a popup with an expanded view of the class. in this expanded view it shoudl show the class details at the top in one line. Then it should have an individual Task list area, where the user can add tasks sepcfiicaly for the class, this task list shoudl also be atuoamtically integrated wtih the full school view task list, so anything added there specific to this class shows up in this task list, and vice versa. Tasks in this view should be added int eh same manner as how tasks are added in the new task list setup I expalined in step 5.  Also in this expanded class view should be a section where the user can upload a document/pdf of their class's syllabus, and there should be the abiltyt to upload their syllabus, view it in a popup screen, remove/swap out the syllabus as well. At the top of this expanded calss view (right beneath the class details) should be a seciton that lists all the assessments from the class, and assessments can be added indiivudally, there should be a list and the categories/column names shoudl be assessment name, type, and date, there should be an Add button whcih shoudl open up a popup, when an Assessment is added, it shoudl prompt the user first for the type of the assessment (Quiz, Exam, Midterm/Final), whenever somethign is added in the assessment tab it shoudl atuomatcially be added inside the indivual class task lists as well as the main task list because we have all the informaiton we need, the class comes directly from wehre they added it and th erst of the info they still input, so everyhig should be interconnected. So just to recap, the structure of this expanded class view should be class details at the top, underneath that should be Assessments on the left and syllabus viewing/adding button on the right, beneath that should be the individual class task list (grouped by today, this week, this month, and future) with filtering options (also each task type should be a different text color), and this all should be in a popup window subscreen.

First since this is all very verbose instructions, store my entire instructions/prompt in a file word by word so you can refernece, and add all of this into your plan for the night, make sure everyghing I gave above is fully implemmented/integrated and verified with your team, and once this is all odne, go ahead an auotmatically deploy, then after EVERYTHING has been compelted and new verison has been deployed, then kill the caffeinate -i command. And make sure that you have an internal task list so you never lose any count of tasks and you never lose any details, if you dont have an internal task/plan list with status' to keep track of each tasks' backlog queue, in developement, testing, verification, iteration, and ocmpelted progress, then make one first that is  apriority.
