# Overnight session 2026-08-23/24 — Ayman's requirements, verbatim

Stored per explicit instruction ("store this entire prompt somewhere with all
the things im askign so you can reference"). **This file is the scope
authority. If a spec or plan disagrees with this file, this file wins.**

Ayman is asleep for the duration. **No questions to him — not from the Lead,
not from any engineer.** All engineer questions route to the Opus Lead.
`caffeinate -i` runs for the session and is killed as the final step.

---

## Part 1 — The Distractions system (three surfaces)

### 1. Record a trigger

> There should be a button called 'Distractions' in the middle of the top bar
> (so it should be psotioned right under where it says Life OS). When you press
> on it, it opens a popup which first prmopts you to select one of the domains,
> once you select a domain the next subscreen on the popup should list out all
> the past triggers in that domain sorted (up to down) in order of
> occurance/frequency that distraction was triggered, above the list shoudl be a
> search bar which can be used to searhc for specific triggers. Then at the top
> of the popup window panel should be a button to add a new trigger. If an
> existing trigger is pressed the panel shoudl close and it shoudl be recorded
> that that speicifc trigger lead to a distrcation that day, if a new trigger is
> added then to add it the user should be prompted with a short textbox to
> explain the trigger, then when they hit save, panel closes and that trigger
> gets added to the list and is counted as a trigger that lead to a distraction
> today. Also unique to when a trigger is being added in the deen domain, in
> addiiton to the list view + search bar + new trigger creation button, on the
> right side should be the reflection module as well which is located in the
> Deen section (the light, moderate, and heavy thing) and this should be
> optinoal so if whatever th eeffect of that trigger was lead to some sort of
> slip or sin the user can conveniently record it right htere, and this should
> update accordingly in the refleciotn module in the deen section

### 2. Home — Focus module gains a Distractions part

> Then inside the home screen, inside the Focus module since distracitons is
> correlated to focus , I want you to move the lock in button to the right of
> the Focus content instead of beneath it, then beneath the Focus part in the
> same module add a distractions part, it should be substitled Distractions and
> should show the number of distractions today, then there should be a Action
> Plan button to the right of that, when pressed it shoudl display triggers in
> order of most recent precendence as well as their plan of action. In this
> seciton the user can edit triggers and plan of actions (but this still isn't
> the nighlty review section, also this section shoudltn display triggers that
> were created that day, beacuse those will still be waiting on action plans, it
> will only display triggers whose actions plans have been created already)

### 3. Nightly review

> Last part should be the nightly review part, after 9 PM an icon should open in
> the top bar on the right side of the Distractions button whic is the center,
> it should be called Review, and when this is clicked it shoudl display all of
> the day's triggers that were added, if the tirgger was a preexisitng one then
> its old plan of aciton shoudl be present and the user should be asked if they
> actually followed through with this plan, if not then the plan remains, and if
> yes than it has to be edited, and for any new trigger they have to add a plan
> of action.

---

## Part 2 — Other things to fix tonight

> 1. I want you to incorprate my class schedule into the school section, as well
> as 'The day's shape' seciton so all my classes are appropriately placed in the
> day's view with all th enecessary informatino when you click on the class.
>
> 2. Make This week's goals section whcih is located at the top of the home
> screen a little better structured, right now its kind of just like colored
> text bbut it should be a little more put together and
> stuructured/formatted/beautificaed to idnicate and clearly display the two
> goals and make this distinct so the user knwosn what is deen what is business
>
> 3. Remove the user profiel icon at the top right section of the top bar, and
> replace that button with a calendar button, and this should open up to this
> week's full calnedar view with everything on it, so all my classes, when I
> have work (from 10:30 - 5:30 on monday, 4:30 - 6 on wedensday, and 7:30 to
> 5:30 on fridays), at the top of the calendar should again be this week's goals
> celary present and well structured, it shoudl also include my fitness/workout
> sessions/routines, and any deadlines/due dates for any of the domains
>
> 4. In teh deen screen, under the refelction module, replace the current
> horizontal insight calednar type feature with a full montly calendar view that
> displays each day colored according to the weigth of whatever was accounted
> durin that day, so basically the more heavier the deeds counted the darker
> shade of red should be displayed, and also in the card for each day it shoudl
> display the count of eahc category, so Light: .. , Moderate: ..., Heavy: ...
>
> 5. In the Habit builder module, replace the current top section where it says
> Pick this weeks focus habit with a simple Create New Habit button. And in this
> habit builder module, in the last 30 days graph, add the dates subtly above
> each square (so just add it one time at the top and each square under that
> date will be for that date), and do the same with the prayer concistency graph
>
> 6. After all these changes are compelte, the last step is remove/delete all
> preexisting app date SPECIFICALLY TIED TO MY DATA, as in whatever I could have
> recorded in ANY screen of any domain, because ill start to actually use this
> app from tommorow, and I havnt used it at all today so i want ALL test data
> and prevoius data form all the domains gone includeing all past qada data and
> prayer data and such so all the isnights will be accurate because now i will
> star to use the app, once you've done all of this go ahead and deploy this as
> well.

> So i will be asleep during this time, so do NOT ask me any question and make
> sure your sonnet engineer agents DO NOT ask me any question at all etiher, all
> their qeustinos go to you, and i will not be awake to asnwer any question
> sduring this session so don't stop and trigger the ask user tool. Also launch
> teh 'caffeinate -i' terminal command so the laptop doenst sleep while you
> complete all these changes,a nd afte reverytihg is done and reviewed you can
> terminate the caffeinate -i command so the latpop can sleep, this shoud bet
> the last step. Also make sure you store this entire ormpt somewhere with all
> the things im askign so you can reference.

---

## Class schedule (from the screenshot Ayman sent mid-turn)

| Course | Room | Instructor | Days | Time |
|---|---|---|---|---|
| CS-3341-HON | ECSN 2.120 | Nicholas Robert Ruozzi | Mon, Wed | 08:30–09:45 |
| CS-3345-HON | FO 2.404 | Andrew Schmidt Nemec | Tue, Thu | 10:00–11:15 |
| PHYS-2326-002 | SCI 1.220 | Mengke Liu | Tue, Thu | 13:00–14:15 |
| PHYS-2126-105 | SCI 1.169 | Lamya Saleh, Paul J. Macalevey | Wed | 13:00–15:45 |
| AMS-2341-HN1 | AD 2.238 | Erin Smith | Tue, Thu | 16:00–17:15 |

## Work schedule (from the prompt)

| Day | Time |
|---|---|
| Monday | 10:30–17:30 |
| Wednesday | 16:30–18:00 |
| Friday | 07:30–17:30 |

No class/work conflicts exist in the above.

---

## Lead's interpretation of item 6 (the wipe) — recorded because Ayman cannot be asked

His stated reason is **insights accuracy**: "so all the isnights will be
accurate because now i will star to use the app." Insights are computed from
*activity history*, not from *configuration*. A literal every-row wipe would
also destroy things he is asking for in this same prompt:

- `weekly_goals` (2 rows) — item 2 asks these very goals be displayed better.
- fitness config (`workout_plans`/`plan_micro_exercises`/`exercises`/`rep_goals`)
  — the Starter Reps plan built last session for him to start using tomorrow.
- `custom_habits` (2) — habit definitions; item 5 rebuilds the Habit Builder.
- `schedule_events` — classes/work being created tonight per items 1 and 3.

**Ruling: wipe recorded activity, preserve configuration.** A full `pg_dump` of
every one of his rows is taken to a gitignored local file BEFORE any delete, so
a literal wipe remains one command away if he wants it in the morning.

DELETE (activity/history): prayer_logs, quran_sessions, sunnah_logs,
reflection_entries, habit_logs, qada data, checkin_allocations, work_sessions +
session hours, workout_sessions + session_sets, body_metrics,
fitness_benchmarks, fitness_cycle_anchor, tasks, coop_tasks, notification_reads.

PRESERVE (configuration): profiles, weekly_goals, custom_habits, exercises,
workouts, workout_plans + plan_*, active_workout_plans, rep_goals,
coop_targets, schedule_events.

This ruling is flagged to Ayman in the morning report.
